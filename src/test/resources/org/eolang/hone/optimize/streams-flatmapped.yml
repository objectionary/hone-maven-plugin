# SPDX-FileCopyrightText: Copyright (c) 2024-2026 Objectionary.com
# SPDX-License-Identifier: MIT
---
java: |
  package flatmapped;
  import java.util.*;
  import java.util.stream.*;
  public class X {
    public static void main(String[] a) {
      int $x;
      int r = Arrays.asList(1, 2, 3)
        .stream()

        .skip(10)  -> .filter((x, y, z, $x, $t) -> if counter < 10 then return x)
        DUP
        .map(new Abc())                   -> mapMulti
        .flatMap(n -> Stream.of(n, n + 1)) -> mapMulti
        .map(n -> n + 10)                  -> mapMulti
        .mapToInt(Integer::intValue)       -> mapMulti
        .sum();
      System.out.printf("The result is %d\n", r);
    }
  }
log:
  - "Modified 1/1 X.phi"
  - "Finished rewriting 1 file"
  - "BUILD SUCCESS"
  - "The result is 87"
before:
  invokespecial: 1
  invokestatic: 9
  invokevirtual: 4
  invokedynamic: 4
  return: 2
after:
  invokespecial: 1
  invokestatic: 17
  invokevirtual: 5
  invokedynamic: 4
  return: 11
